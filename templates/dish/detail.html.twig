{% extends 'base.html.twig' %}

{% block title %}{{ dish.title }}{% endblock %}

{% block body %}

    <section class="py-5">
        <h1>{{ dish.title }}</h1>
        <div class="row gx-4 gx-lg-5">
            <div class="col col-6 col-md-4 mb-5">
                <div class="card h-100">
                    <img class="card-img-top" src="{{ asset('images/' ~ dish.image) }}" alt="..." />
                </div>
            </div>
            <div class="col col-6 col-md-4 mb-5">
                <div class="p-4">
                    <div class="">
                        <div>{{ dish.description }}</div>
                        <p>€ {{ dish.price }}</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

{% endblock %}
